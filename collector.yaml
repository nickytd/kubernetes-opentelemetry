mode: deployment
config:
  exporters:
    logging:
      loglevel: info
    otlp:
      endpoint: ofd-data-prepper.logging.svc.cluster.local:21890
      tls:
        insecure: true

ports:
  metrics:
    enabled: true
serviceMonitor:
  enabled: true

config:
  exporters:
    logging:
      loglevel: info
    otlp:
      endpoint: ofd-data-prepper.logging.svc.cluster.local:21890
      tls:
        insecure: true
  service:
    pipelines:
      traces:
        exporters:
          - otlp